//This is the server that ondemand will initially look for the dataset, and to which the user will be forwarded after it's converted
VISUSSERVER='http://gunship.sci.utah.edu:8080'
  //NOTE It's the same as VISUSSERVER, specified in ondemand-cfg.sh. Is there any way to read the environment instead of having a dup cfg?
  